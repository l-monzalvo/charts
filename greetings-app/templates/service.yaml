apiVersion: v1
kind: Service
metadata:
  name: {{ include "greetings-app.fullname" . }}
  labels:
    {{- include "greetings-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "greetings-app.selectorLabels" . | nindent 4 }}
